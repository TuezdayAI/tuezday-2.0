"use client";

import type { WizardPanelProps } from "./types";

/** Sprint 36.6 stub — replaced by the campaign-panel agent. */
export function CampaignPanel(_props: WizardPanelProps) {
  return <p className="subtitle">Loading…</p>;
}
