"use client";

import type { WizardPanelProps } from "./types";

/** Sprint 36.5 stub — replaced by the brain-panel agent. */
export function BrainPanel(_props: WizardPanelProps) {
  return <p className="subtitle">Loading…</p>;
}
