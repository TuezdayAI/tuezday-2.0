"use client";

import type { WizardPanelProps } from "./types";

/** Sprint 36.6 stub — replaced by the draft-panel agent. */
export function DraftPanel(_props: WizardPanelProps) {
  return <p className="subtitle">Loading…</p>;
}
