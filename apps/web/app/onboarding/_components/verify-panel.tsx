"use client";

import type { WizardPanelProps } from "./types";

/** Sprint 36.5 stub — replaced by the verify-panel agent. */
export function VerifyPanel(_props: WizardPanelProps) {
  return <p className="subtitle">Loading…</p>;
}
