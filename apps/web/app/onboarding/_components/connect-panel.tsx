"use client";

import type { WizardPanelProps } from "./types";

/** Sprint 36.5 stub — replaced by the connect-panel agent. */
export function ConnectPanel(_props: WizardPanelProps) {
  return <p className="subtitle">Loading…</p>;
}
